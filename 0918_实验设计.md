你说得对。前面给出的“类别场、知识图、点过程、Universal DA”仍然是一个**研究框架**，还没有形成一个能够独立命名、直接实现、推导性质并与现有算法正面对照的核心算法。

下面我给出一版真正收敛的候选算法。它不再以 OT、GNN 或 prototype transport 为中心，而是直接回答你最核心的问题：

> **目标港口没有单船 AIS 时，港口知识相对于 SAR 视觉到底增加了多少类别证据；哪些源港口—类别成分能够解释目标船舶；不能解释的部分是否对应目标私有类别。**

暂用工作名：

# **KIRC：Knowledge-Increment Ratio Closure**

## 知识增量比率闭合分解

名字可以以后更换。核心不是名字，而是两个新的计算步骤：

$$\boxed{ \text{知识增量比率算子} + \text{已知类别闭合—未知残差分解} }$$

它直接输出：

- 知识相对视觉增加的类别证据；
    
- 每个源港口、每个源类别对目标港口的贡献；
    
- 目标中缺失的源私有类别；
    
- 目标中无法被源类别解释的目标私有类别。
    

---

# 一、为什么从这个切入，而不是继续做知识图运输？

你当前实验已经暴露出一个非常明确的问题：

- 整体目标条件化适配相对视觉 guide 有一定收益；
    
- 但 K 相对 noK 的 BA 增量只有约 0.0370.037 个百分点；
    
- K 与 wrongK 在 21,195 个样本中只改变了 3 个最终类别预测。
    

这说明当前缺少的不是更多知识特征，而是一个能够回答下面问题的运算：

$$\boxed{ \text{加入港口知识以后，类别 }c\text{ 的证据究竟增加了多少？} }$$

普通拼接、FiLM、GNN 和 cross-attention 会直接学习：

f(z,k),f(z,k),

但无法区分：

- 视觉 zz 已经包含的证据；
    
- 港口知识 kk 真正新增的证据；
    
- 仅仅表示港口 ID 的捷径；
    
- 视觉和知识之间真实的条件作用。
    

KIRC 首先解决这个分离问题。

---

# 二、数据对象

类别无关检测器先从目标大景中提取船舶候选。

对每个船舶 ii，定义：

$$z_i=E_X(x_i)$$

为 SAR 视觉特征。

定义目标端实际可用的实例相关知识：

$$k_i= \left( k_i^{\mathrm{spatial}}, k_i^{\mathrm{local}}, k_i^{\mathrm{relation}} \right),$$

例如：

- 到岸线、航道、锚地、港口入口的距离；
    
- 船首方向与航道方向夹角；
    
- 局部船舶密度；
    
- 当前船舶所在的语义海域；
    
- 与局部港口图的关系。
    

目标端不可用的单船 AIS 类型、MMSI、目的地等不能进入$k_i$。

港口级知识与元数据单独记为：

$$d_p= \left( d_p^{\mathrm{meta}}, d_p^{\mathrm{facility}}, d_p^{\mathrm{function}}, d_p^{\mathrm{scene}} \right).$$

这里要坚持类型分工：

-$k_i$ 用于判断具体船舶—场景关系；
    
-$d_p$ 用于判断哪些源港口、源类别成分适合解释目标港口；
    
- 不把港口全局设施向量直接复制给每艘船。
    

---

# 三、第一个核心：知识增量比率算子

设源港口为 pp，类别为 cc。

定义源港口—类别成分：

g=(p,c).g=(p,c).

它在视觉空间中的分布记为：

$$P_g^V,$$

在视觉＋知识联合空间中的分布记为：

$$P_g^{VK}.$$

目标港口无标签分布分别记为：

$$P_t^V, \qquad P_t^{VK}.$$

定义两个密度比：

$r_g^V(z) = \frac{dP_g^V}{dP_t^V}(z)$,$r_g^{VK}(z,k) = \frac{dP_g^{VK}}{dP_t^{VK}}(z,k)$.

然后定义核心运算：

$$\boxed{ \Delta_g(z,k) = \log r_g^{VK}(z,k) - \log r_g^V(z). } \tag{1}$$

利用条件概率分解：

$$p(z,k)=p(z)p(k\mid z),$$

可以严格得到：

$$\boxed{ \Delta_g(z,k) = \log \frac{ p_g(k\mid z) }{ p_t(k\mid z) }. } \tag{2}$$

这就是**港口知识相对于视觉的增量比率**。

---

## 这个运算到底解决了什么？

### 情况一：知识没有新增信息

若：

$$p_g(k\mid z)=p_t(k\mid z),$$

则：

$$\Delta_g(z,k)=0.$$

知识不应改变视觉判断。

### 情况二：只有视觉域发生变化

即：

$$p_g(z)\neq p_t(z),$$

但条件知识关系不变：

$$p_g(k\mid z)=p_t(k\mid z).$$

联合密度比和视觉密度比中的视觉变化会相减：

$$\Delta_g=0.$$

因此不会把普通视觉域偏移错误解释成知识作用。

### 情况三：知识—视觉关系发生变化

如果同样的 SAR 视觉特征，在源港口与目标港口中对应不同的空间／功能关系：

$$p_g(k\mid z)\neq p_t(k\mid z),$$

则：

$$\Delta_g\neq0.$$

这才是知识真正应介入的地方。

---

# 四、如何估计这个算子？不需要显式估计高维密度

对每个源港口—类别成分 g=(p,c)g=(p,c)，训练两个二分类密度比估计器。

## 视觉估计器

输入：

z.z.

区分：

- 来自源成分 gg；
    
- 来自无标签目标港口。
    

得到：

$$D_g^V(z).$$

## 视觉—知识联合估计器

输入：

(z,k).(z,k).

区分：

- 来自源成分 gg；
    
- 来自无标签目标港口。
    

得到：

$$D_g^{VK}(z,k).$$

在平衡采样时：

$\widehat r_g^V(z) = \frac{D_g^V(z)} {1-D_g^V(z)}$,$\widehat r_g^{VK}(z,k) = \frac{D_g^{VK}(z,k)} {1-D_g^{VK}(z,k)}$.

采样比例不平衡时加入已知的类别先验校正。

最终：

$$\boxed{ \widehat\Delta_g = \operatorname{logit}D_g^{VK} - \operatorname{logit}D_g^V. } \tag{3}$$

实际实现不需要为每个成分训练完全独立的网络，可以使用一个共享 encoder 和$P\times C$ 个轻量 ratio heads。

密度比估计本身是成熟工具；ICML 2024 还专门研究了密度比估计中的误差饱和，并把改进方法用于深度 UDA 模型加权。最近的 TransPL 也通过显式类条件转移结构和 Bayes 规则生成 UDA 伪标签。**KIRC 的创新不能写成“首次使用密度比或 Bayes”，而是式（1）的知识增量分离，以及后面的 Universal 标签空间闭合分解。** ([Proceedings of Machine Learning Research](https://proceedings.mlr.press/v235/gruber24b.html?utm_source=chatgpt.com "Overcoming Saturation in Density Ratio Estimation by Iterated Regularization"))

---

# 五、第二个核心：已知类别闭合—未知残差分解

这一步同时处理：

- 多源港口选择；
    
- shared classes；
    
- source-private classes；
    
- target-private classes。
    

设：

$$\beta_{pc}\ge0$$

表示目标港口中，可由源港口 pp 的类别 cc 成分解释的质量。

若目标联合分布可以表示为：

$$P_t^{VK} = \sum_{p,c} \beta_{pc}P_{pc}^{VK} + \beta_uP_u,$$

其中$P_u$ 表示目标私有类别分布。

两侧除以目标分布$P_t^{VK}$，得到：

$$\boxed{ 1 = \sum_{p,c} \beta_{pc} r_{pc}^{VK}(z,k) + \rho_u(z,k), } \tag{4}$$

其中：

$$\rho_u(z,k) = \beta_u \frac{dP_u}{dP_t^{VK}}(z,k) \ge0.$$

这条恒等式非常重要。

它说明：

> 目标样本的概率质量，要么由某个源港口—已知类别成分解释，要么进入非负的未知残差。

这不是在 softmax 上设一个人工阈值。

---

# 六、目标端求解问题

对目标港口的$N_t$ 个船舶候选，计算：

$$R_{i,pc} = \widehat r_{pc}^{VK}(z_i,k_i).$$

求：

$$\boxed{ \begin{aligned} \min_{\beta,\rho}\quad & \frac1{N_t} \sum_{i=1}^{N_t} \phi \left( \sum_{p,c} \beta_{pc}R_{i,pc} + \rho_i \right) \\ &+ \lambda_u \frac1{N_t} \sum_i\rho_i \\ &+ \lambda_K \sum_{p,c} \beta_{pc}\, d_K(d_p,d_t) \\ &+ \lambda_C \sum_c \|\beta_{\cdot c}\|_2 + \frac{\eta}{2}\|\beta\|_2^2 \\[2mm] \text{s.t.}\quad & \beta_{pc}\ge0, \qquad \rho_i\ge0, \\ & \sum_{p,c}\beta_{pc}\le1. \end{aligned} } \tag{5}$$

取：

$$\phi(s)=s-\log s-1.$$

它在：

s=1s=1

时最小，正好对应闭合恒等式（4）。

---

## 每一项的含义

### 闭合项

$$\phi\left( \sum\beta R+\rho \right)$$

要求已知解释质量与未知残差之和接近 1。

### 未知残差代价

$$\lambda_u\sum_i\rho_i$$

避免把所有目标样本都简单判为 unknown。

### 港口知识匹配

$$d_K(d_p,d_t)$$

由目标可见信息计算，例如：

- 元数据距离；
    
- 港口功能距离；
    
- 航道／锚地结构距离；
    
- 船舶群体统计距离；
    
- 设施构成距离。
    

它只作为源港口成分选择的软约束，不直接决定船型。

### 类别组稀疏

$$\sum_c\|\beta_{\cdot c}\|_2$$

允许某个源类别在目标港口完全消失：

$$\beta_{\cdot c}=0.$$

这对应 source-private 类抑制。

### 强凸项

$$\frac\eta2\|\beta\|^2$$

使固定比例估计器条件下的解唯一。

---

# 七、这个问题具有全局最优解

固定密度比估计以后：

-$\phi$ 是凸函数；
    
-$\sum\beta R+\rho$ 对变量是仿射；
    
- 组范数是凸的；
    
- 港口知识项是线性的；
    
- 可行集是凸集；
    
-$\eta>0$ 时对$\beta$ 强凸。
    

因此式（5）是一个凸优化问题。

不是：

$$\text{“训练到一个不知道好不好的局部驻点”}.$$

固定$\beta$ 时，未知残差甚至有闭式更新。

令：

$$e_i = \sum_{p,c} \beta_{pc}R_{i,pc}.$$

则：

$$\boxed{ \rho_i^* = \left[ \frac1{1+\lambda_u} - e_i \right]_+. } \tag{6}$$

因此只需要优化$\beta$，$\rho$ 可以直接计算。

这已经是一个完整的核心算法，而不是概念框架。

---

# 八、最终分类和 unknown 输出

目标样本 ii 被类别 cc 解释的质量：

$$q_{ic} = \sum_p \widehat\beta_{pc} R_{i,pc}.$$

归一化后：

$$\boxed{ P(Y_i=c\mid x_i) = \frac{ q_{ic} }{ \widehat\rho_i+\sum_{c'}q_{ic'} }. } \tag{7}$$

Unknown 概率：

$$\boxed{ P(Y_i=\mathrm{unknown}\mid x_i) = \frac{ \widehat\rho_i }{ \widehat\rho_i+\sum_{c'}q_{ic'} }. } \tag{8}$$

目标中某个源类别的总体存在质量：

$$\boxed{ \alpha_{t,c} = \sum_p\widehat\beta_{pc}. } \tag{9}$$

于是：

-$\alpha_{t,c}\approx0$：source-private 类；
    
-$P(Y_i=c)$ 高：shared 类；
    
-$\rho_i$ 高：target-private 候选；
    
- 同一 unknown 残差中的样本可以继续聚类和 VLM 命名。
    

---

# 九、知识增量怎样进入最终判断？

联合比率可分解为：

$$\widehat r_{pc}^{VK} = \widehat r_{pc}^{V} \exp \left( \widehat\Delta_{pc} \right).$$

所以目标类别质量为：

$$q_{ic} = \sum_p \beta_{pc} \widehat r_{pc}^{V}(z_i) \exp \left[ \widehat\Delta_{pc}(z_i,k_i) \right]. \tag{10}$$

这条式子非常适合做消融。

### Visual-only KIRC

令：

$$\Delta_{pc}=0.$$

### 完整 KIRC

使用真实：

$$\Delta_{pc}(z_i,k_i).$$

二者差异就是：

$$\boxed{ \text{知识相对于视觉的独立增量}. }$$

这比 K/noK 两个完全独立训练的模型更干净，因为：

- 视觉比率相同；
    
- 类别分解相同；
    
- 优化器相同；
    
- 只移除知识增量算子。
    

---

# 十、怎样避免港口 ID 和知识边际捷径？

可以在源港口 meta-training 中增加一个配对交换检验。

定义任意得分$f_c(z,k)$ 的交换量：

$$\boxed{ \mathfrak X_c = f_c(z_i,k_i) + f_c(z_j,k_j) - f_c(z_i,k_j) - f_c(z_j,k_i). } \tag{11}$$

如果：

$$f_c(z,k) = a_c(z)+b_c(k)+h_c(p),$$

则：

$$\mathfrak X_c=0.$$

所以它会消除：

- 视觉单独作用；
    
- 知识边际单独作用；
    
- 港口 ID 偏置。
    

只有真实的：

$$z\times k$$

交互才能产生非零交换量。

对确实具有实例对应关系的知识，可以加入：

$$\mathcal L_{\mathrm{exchange}} = \log \left[ 1+ \exp \left( m-\mathfrak X \right) \right]. \tag{12}$$

负配对应当在同港口、相近成像条件和相近尺度中构造，不能简单把另一个港口都当错误关系。

这一项不是算法主体，但能够防止知识比率估计器只学习港口捷径。

---

# 十一、完整算法流程

## Source training

1. 训练类别无关船舶检测器；
    
2. 训练视觉特征编码器；
    
3. 构造目标端也可获得的类型化知识；
    
4. 在源港口伪目标 episodes 中训练共享 ratio encoder；
    
5. 使用 exchange loss 防止边际捷径；
    
6. 所有超参数只在源港口 leave-one-port-out 中确定。
    

## Target adaptation

1. 在目标大景中检测海面船舶；
    
2. 提取$z_i,k_i,d_t$；
    
3. 对每个源港口—类别成分估计：
    
  $r_{pc}^V,\quad r_{pc}^{VK}$;
4. 计算知识增量：
    
  $\Delta_{pc}$;
5. 求解凸闭合分解式（5）；
    
6. 输出 known 类和 unknown；
    
7. 将 unknown 进行聚类和可选 VLM 命名。
    

---

# 十二、伪代码

```text
INPUT:
    labeled source ports {Dp}
    unlabeled target scene Dt
    target metadata and port knowledge Kt

SOURCE:
    train class-agnostic detector
    train visual encoder EX
    construct typed local knowledge encoder EK

TARGET:
    detect target ships {bi}
    zi = EX(crop(I_t, bi))
    ki = EK(location_i, G_t, M_t)

FOR each source port p and source class c:
    train visual ratio head:
        source (p,c) vs unlabeled target, input z

    train joint ratio head:
        source (p,c) vs unlabeled target, input (z,k)

    log_rV[p,c]  = corrected_logit(DV[p,c])
    log_rVK[p,c] = corrected_logit(DVK[p,c])
    Delta[p,c]   = log_rVK[p,c] - log_rV[p,c]

R[p,c,i] = exp(log_rVK[p,c](zi,ki))

solve convex closure:
    beta, rho = argmin J(beta,rho)

FOR each target ship i:
    known_score[c] = sum_p beta[p,c] * R[p,c,i]
    unknown_score  = rho[i]
    normalize known_score and unknown_score

OUTPUT:
    boxes
    shared-class labels
    unknown scores
    source-private class masses
```

---

# 十三、为什么它比“类型化 OT＋残差”更独立？

|旧方案|KIRC|
|---|---|
|运输特征或 prototype|估计源成分相对目标的概率比率|
|知识作为运输代价|知识被定义为视觉之外的条件证据|
|unknown 是 OT 未匹配质量|unknown 是闭合恒等式中的非负概率残差|
|视觉和知识容易重复计数|联合比率减视觉比率，显式去除重复视觉证据|
|source-private 后处理抑制|类别成分权重$\beta_{\cdot c}$ 在统一凸问题中趋零|
|一般依赖迭代局部解|固定 ratio 后闭合分解是强凸问题、解唯一|

它不是“OT 加几个正则项”。

---

# 十四、可以写成论文主定理的四个结果

## 定理 1：知识增量恒等式

$$\Delta_{pc} = \log \frac{ p_{pc}(k\mid z) }{ p_t(k\mid z) }.$$

证明知识增量不包含纯视觉边际漂移。

## 定理 2：Universal 闭合恒等式

$$1 = \sum_{p,c} \beta_{pc}r_{pc}^{VK} + \rho_u.$$

known、source-private 和 target-private 在同一式子中出现。

## 定理 3：内层全局最优性

式（5）在固定 ratio 下为强凸问题，存在唯一全局最优解；未知残差具有闭式更新（6）。

## 定理 4：一致性或误差界

争取证明：

$$\|\widehat\beta-\beta^*\| + \frac1N \sum_i |\widehat\rho_i-\rho_i^*| \le C \left( \epsilon_{\mathrm{ratio}} + \epsilon_{\mathrm{knowledge}} + \epsilon_{\mathrm{det}} \right).$$

其中：

-$\epsilon_{\mathrm{ratio}}$：密度比估计误差；
    
-$\epsilon_{\mathrm{knowledge}}$：知识与空间关系误差；
    
-$\epsilon_{\mathrm{det}}$：大景检测误差。
    

---

# 十五、最小决定性实验

先固定 detector 和视觉 encoder，只验证 KIRC 核心。

|方法|检验内容|
|---|---|
|Visual classifier|无知识、无适应|
|Concatenate K|普通知识融合|
|FiLM / GNN K|网络型知识融合|
|Visual density ratio|只有$r^V$|
|Joint density ratio|只有$r^{VK}$|
|Ratio di$f$erence，无闭合|验证$\Delta$|
|Closure，无知识增量|$\Delta=0$|
|**完整 KIRC**|$\Delta+\beta+\rho$|

必须同时测试：

- correct target knowledge；
    
- wrong-port knowledge；
    
- within-port shuffled relations；
    
- metadata only；
    
- local knowledge only；
    
- global port knowledge only；
    
- complete typed knowledge。
    

---

# 十六、核心成功标准

KIRC 至少需要满足：

1.$\Delta$ 相对普通 concat/GNN 更能预测真实知识 rescue–harm；
    
2. correct K 明显优于 shuffled K 和 wrong-port K；
    
3. source-private 类的$\alpha_{t,c}$ 接近零；
    
4. target-private 类的$\rho_i$ 高于低质量 shared 类；
    
5. 在相同 shared-class BA 下，unknown recall 高于 energy/prototype/UDA 基线；
    
6. Visual-only closure 到完整 KIRC 的增益在多个港口稳定存在；
    
7. worst-port 不明显恶化；
    
8. 港口 ID 不能替代类型化知识。
    

---

# 十七、明确失败判据

下列任一情况出现，应撤回或收紧对应创新主张：

-$\Delta\approx0$，说明知识没有独立条件证据；
    
- joint ratio 与 ratio difference 表现相同，说明“去除视觉重复证据”没有价值；
    
- wrong-port K 与 correct K 几乎一致；
    
-$\beta$ 无法抑制目标中不存在的源类别；
    
-$\rho$ 主要识别低 SNR、小目标，而不是真实 target-private 类；
    
- 普通 energy threshold 达到相同 unknown 性能；
    
- concat/GNN 达到相同效果；
    
- 结果只在同一大景 transductive 设置中成立；
    
- ratio estimator 在不同随机种子或不同源端口混合下不稳定。
    

---

# 最终判断

**这才是一版真正可以称为“核心算法候选”的方案。**

它不是：

$$\text{新任务} + \text{知识图} + \text{OT} + \text{unknown threshold}.$$

而是一个统一计算过程：

$$\boxed{ \text{视觉密度比} \rightarrow \text{联合密度比} \rightarrow \text{知识增量比率} \rightarrow \text{源港口—类别闭合分解} \rightarrow \text{目标私有残差}. }$$

最关键的新算子是：

$$\boxed{ \Delta_{pc}(z,k) = \log r_{pc}^{VK}(z,k) - \log r_{pc}^{V}(z), }$$

最关键的新求解问题是：

$$\boxed{ 1 = \sum_{p,c} \beta_{pc}r_{pc}^{VK} + \rho_u. }$$

前者回答“知识新增了什么”，后者回答“哪些已知类别能够解释目标，哪些部分必须留给 unknown”。

下一步正确的开发顺序不是先建复杂的大景模型，而是：**在现有检测框或已有 chip 上实现 KIRC 的 ratio heads 和凸闭合求解，用受控 hidden-class 实验检验这两个核心式子。**只有它们通过，再扩展到完整 Sentinel-1 大景检测。