# HANDOFF — 当前状态（供任何新会话接手）

**更新**：2026-09-26 中午；唯一真源 `EXPERIMENT_STATE.md`（E12–**E26a**）；镜像仓库已推至 `e47063b`。

## 1. 一句话
> **跨港迁移保留「序」、破坏「值」** ⇒ 阈值/保证类闸门结构性死路 ✗；可部署形态 = **序 + 预算** ✓；
> 且**机制跨任务成立 ✓**（非 SAR 任务 5/5 域 ✓）；**但方法向的"新构件"已被前作占满** ✗（见 §4）。

## 2. 可主张清单（每条过判据或有对照实测 ✓）
| 主张 | 证据 |
|---|---|
| 决策层策略**收回适配伤害** | E24b ✓（23/24 港 ✓ 10% 预算即回到 −0.0012 ✓） |
| **两种标准 UDA 在本设定无效**（CORAL 净负 ✗ / 伪标中性 ✗） | e185 + e186 ✓ |
| 机制在**四套外部数据**成立 | OpenSARShip1 ✓ private7 ✓ **OpenSARShip2 5/5 ✓✓** ｜ **NASTaR 部分 ⚠**（10% 5/6 ✓ 20% 4/6 ✗） |
| 机制**跨任务**成立 | e183 ✓（非 SAR 10 类 ✓ 5/5 域 ✓） |
| **AIS-free 的信息论代价** | +4.23 vs +0.37 ✓ 零相关 ✓ 池化零增益 ✓ |
| **完整阴性表 + 方法学**（chip 级可复现/类级不可信 ✓） | E17–E26 ✓ |
| **F_0921 语料已对齐 + 去近岸** | E26a ✓（6,989 景三层 100% ✓；237,577 点保留 ✓） |

## 3. 已知的"更强版本被否"（三次，均实测）
深模块 ✗ ｜ 可部署判据（预测收益符号）✗ ｜ 插件提升（在中性 UDA 上加分）✗ ⇒ **只写实测支持的那一档** ✓。

## 4. 新颖性红线（必须遵守 ✓）
- **不可主张** ✗：域内序 ✗（保形/自适应保形 ✓ 已有）｜ 偏移下选择性分类 ✗（**Liang 2024** TMLR ✓）｜ 偏移下保形的界 ✗（arXiv 2602.14913 ✓）｜ 层级选择性分类 ✗（arXiv 2405.11533 ✓）｜ **预算式弃答 ✗（arXiv 2510.26706 ✓）** ｜ 置信度级联弃答 ✗（Jitkrittum 2023 ✓）｜ 粗到细提升闭集 ✗（CVPR 2024 ✓）；
- **可主张** ✓：**设定**（跨港 SAR + AIS-free + 目标标签严格隔离 ✓）+ **测量**（排序 vs 标定联合量化 + 不可行性曲线 ✓）+ **AIS-free 的信息论代价** ✓ + **阴性表/方法学** ✓；
- ⇒ **venue 判断**：方法向顶会**不成立** ✗；**TGRS/ISPRS** 成立 ✓（设定+测量+边界即贡献 ✓）。

## 5. 待办（等用户一句话 ✓）
1. **B 的判据**：`d<2×LOA` ✓ 现状 / 加 `LOA<100m` / 用泊位语义 —— 三选一 ✓（现状已产出 `f0921_kept.csv` 237,577 点 ✓）；
2. **论文**：按 §4 收缩全文新颖性表述 + 与四个最近邻逐条写"差别" ✓（缺件清单在 `related_papers/README_缺件清单.md` ✓ 6 篇待下 ✓）；
3. **扩池（可选）**：AutoDL 单卡 4090 24GB ✓ + 数据盘分片流转 ✓ + **云上只做检测** ✓（图不必上传，云上重下 ✓）；F_0921 已给出 23.8 万可用点 ✓✓。

## 6. 环境/运维
- 内存已腾 ~4 GB ✓；**重活一次只跑一个** ✓；
- **`NO_PROXY` 已含 `open.feishu.cn`** ✓（修掉 gateway 侧投递静默失败 ✓）；
- 静默守护 cron `2c08cff909cf` ✓；仓库同步 cron `5b88a12a07bc`（每 15 分钟 ✓）；
- **PIL 读 GeoTIFF 在本机会静默硬崩** ✗（`np.array(im)` 无 traceback、exit 127 ✓）⇒ **一律用 `tifffile`/`imageio`/`cv2`/`skimage`** ✓；
- **`osgeo` 只在 ClearSAR python** ✓；anaconda 是 **Py 3.13.9** ✓；
- **下载要校验内容而非退出码** ✗（曾把 6.4 KB HTML 当 zip ✓）⇒ 校验魔数+体积 ✓；
- git 推：`env -u *_PROXY git -c http.proxy= -c https.proxy= push` ✓，提交信息用 `-F` 落盘 ✓（内联引号会炸 ✗）。
