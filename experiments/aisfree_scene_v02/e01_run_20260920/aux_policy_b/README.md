# (b) 决策粒度策略 v1（源端训练，目标端评估）

**纪律**：所有策略只在**源端**拟合（源端真标签 2,971–5,822/折），目标端零真值参与；目标端评估用两种真值视图
（`fine` = 冻结细标签；`coarse` = boost_v2 标签投影到父类，概率**先父类求和后 argmax**）。

## 一、结果（目标端，Acc/BA）

| 折 | 空间 | n | B0（冻结选头） | B1（8头等权） | FIX（源端选最优固定子集） | **STK（源端训练逐对象堆叠）** | OBJ（逐对象 oracle，仅参考） |
|---|---|---:|---|---|---|---|---|
| Rotterdam | fine | 46 | .478/.496 | .435/.479 | .413/.470 | .435/.479 | .500 |
| Rotterdam | coarse | 340 | .259/.295 | .247/.278 | .238/.269 | .247/.271 | **.303** |
| Shanghai | fine | 430 | .709/.684 | .707/.682 | .709/.684 | **.740/.697** | **.786** |
| Shanghai | coarse | 631 | .688/.680 | .686/.679 | .688/.680 | **.721/.692** | **.756** |
| Port Klang | coarse | 181 | .138/.195 | .171/.204 | .193/.211 | .155/.175 | **.287** |
| Fujairah | coarse | 189 | .122/.348 | .111/.289 | .116/.344 | .201/.230 | .153 |
| Jebel Ali | coarse | 997 | .241/.488 | .276/.511 | .278/.503 | .290/.493 | **.335** |
| Port Said | coarse | 189 | .201/.226 | .259/.255 | .217/.234 | .259/.249 | **.413** |

（fine 视图 n=4–46 的三折样本过小，只登记不作结论。）

## 二、三条读数

1. **B0 ≈ B1 ≈ FIX 全部在噪声内**（差 ≤.03）：选哪个头、选哪个固定子集、等权平均，对目标端结果几乎无区别。
   → 与上界诊断一致：**固定子集族没有可利用的空间**。
2. **源端训练的逐对象堆叠（STK）只在 Shanghai 上是稳定正收益**（+.035 acc / +.013 BA）；
   其余折为持平或正负相抵（Port Klang −.016/−.029、Fujairah +.090 acc 但 −.059 BA、Jebel Ali +.014/−.018）。
   → **一个朴素的源端条件策略并不能稳定超过现在的聚合方式。**
3. **OBJ（逐对象 oracle）在所有视图上都显著高于任何可用策略**（+.047 ~ +.154）。
   → 逐对象空间**确实存在**，但**源端训练的简单选择器拿不到它**。这正是「决策粒度」这一层的真实难度：
   不是「换个子集」的问题，而是需要**逐对象可迁移的选择信号**（源端可信、目标端可迁移）。

## 三、结论与后续

- **可以停止**：调 tau、换固定子集、换等权/单头聚合（三条都已被证明没有空间）。
- **(b) 的下一步**（若要继续）：构造**逐对象可迁移的选择信号**并做源港留一验证；本轮已证明「直接源端训练 LR 堆叠」不够。
- 与 (a) 的交叉结论：域间隙（(a)）比决策粒度缺口（(b)）大一个量级 → **优先级应给表示/适配层**。

## 四、文件

- 逐折 JSON：`{fold}_policy_b.json`（含源端指标、固定子集 bitmask、STK 的 rescue/break 计数）
- 脚本：`e01_policy_b.py`
