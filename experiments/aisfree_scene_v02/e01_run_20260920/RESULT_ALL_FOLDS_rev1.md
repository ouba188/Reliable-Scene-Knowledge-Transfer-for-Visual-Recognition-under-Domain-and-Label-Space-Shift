# E01 rev1 四折总汇总（2026-09-21 03:40）

冻结实现 sha256（每折日志首行均有记录）：
`chips=b4c5964178f2 features=ff926d1685d3 glue=e72fa557f276 core=cd207624d7f1`

## 一、四折结果（同一规则、逐折独立 fit）

| 折 | 词表 | 源标签 | 启用维 | cal 港数 | tau | retained | eval 可用 | 可评价 n | B0 Acc/BA | B1 Acc/BA | Full Acc/BA | rescue/harm |
|---|---|---:|---|---:|---:|---:|---:|---:|---|---|---|---|
| Rotterdam | 5 类 | 2,918 | D0,D3 | 4 | 0.244 | 8/8 | 13,200 (0 I/O) | 46 | .478/.496 | .435/.479 | .435/.479 | 0/0 |
| Shanghai | 2 类 | 612 | D0,D1,D3 | 4 | 0.315 | 8/8 | 26,653 (0 I/O) | 430 | .709/.684 | .707/.683 | .707/.683 | 0/0 |
| Port Klang | 6 类 | 5,523 | 见 relation_manifest | 4 | 0.401 | 8/8 | 3,074 (0 I/O) | 9 | .111/.042 | .111/.042 | .111/.042 | 0/0 |
| Fujairah | — | — | 见 relation_manifest | 4 | — | 8/8 | 3,460 (0 I/O) | 7 | .143/.333 | .143/.333 | .143/.333 | 0/0 |

## 二、三个共性事实（只登记，不作方法结论）

1. **全部 retained=8 ⇒ Full ≡ B1**：四折的知识筛选在任何 tau 下都没有剪枝。B0 与 B1 的差异（Rotterdam −.04、Shanghai −.002）都远小于可评价样本量能支撑的分辨率。
2. **可评价标签支撑是主瓶颈**：7–430。固定分母覆盖显示三折 0 个对象因 I/O 被排除（valid_joint 全 1.000），因此瓶颈**不是图像质量**，而是 AIS 细标签在 P0 候选池里的稀有性（Rotterdam eval 13,200 里只有 46 个词表内标签 = 0.35%）。
3. **维启用逐折不同**（D1 只在 Shanghai 达标），符合「源端支撑决定」的设计，不得跨折复用。

## 三、与 rev0 pilot 的关系

rev0 的 chips 因 `ds.index` 行列写反而位置转置（实测：落盘芯片与交换窗口 100% 相同），
rev1 全链重抽重训。**rev1 的改善属于数据修复，不计作方法贡献**；rev0 目录保留为 pilot。

## 四、本轮修复清单（全部有实测证据）

| 问题 | 证据 | 修复 |
|---|---|---|
| crop 行列写反 | 落盘 vs 交换窗口 100% 相同、修正窗口中心饱和 | `row,col=ds.index()`；329 分片重抽，valid 全 1.000 |
| id 截断 | 产品名 68 / 对象 id 95 字符 vs `<U64` | 全部 `<U128` + 唯一性断言 |
| np.savez 后缀坑 | `.npz.tmp` 被写成 `.npz.tmp.npz` → rename 失败 | 文件对象写入 + 原子 rename + `.done` 标记 |
| 训练池随机访问 | 每 epoch 258 s | RAM 池 + 平衡采样 → 7 s/epoch（30 epoch 212 s） |
| 采样非平衡 | 产品分块 ≠ uniform_port_class | P(i)=1/(P·C_p·N_(p,c)) 有放回 + 逐 epoch 抽样落盘 |
| 增广环绕/固定 | np.roll + 固定种子 | reflect-pad + 主进程逐 draw 种子 |
| BA 实为 Acc | 逐港 Acc 平均 | 逐港宏平均召回→跨港平均（B0 选择+指标一致） |
| 校准港映射 | meta 以 object_id 为键误用 | split_manifest 产品→港映射 + 逐港支撑报告 |
| CRLF 换行 | shell 静默退出 | LF 上传 + `bash -n` 校验 |
| 折名带空格 | Port Klang 两次失败（重定向/引号） | 全路径加引号（`set -Eeuo pipefail` 保留） |

## 五、仍待办（下一轮）

- P1 深化：关系误差分解（按 B×D 的观测/预测矩、零值比例、空间块）、每 H 的 meta pairwise disagreement、每 J 预测矩差。
- P2 深化：Shanghai（n=430）可作为唯一有分辨力的折做 rescue/harm 归因；其余三折需先解决标签支撑。
- 标签支撑方案待 GPT 裁定（是否补充「词表外标签→粗类」辅助评价，单列不改主表）。
