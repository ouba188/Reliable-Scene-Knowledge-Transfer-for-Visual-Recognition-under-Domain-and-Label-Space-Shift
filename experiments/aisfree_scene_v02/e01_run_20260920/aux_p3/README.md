# P3 归因（aux_p3，2026-09-21 05:55）

复用四折冻结产物（`filter_inputs.npz` / `eval_probabilities.npy` / `groups` / `calibration.json`），
**不训练、不改规则、模型输入不含任何目标标签**。评估用 eval 侧的独立概率数组（`[H,N_eval,C]`）。

## 变体读数（同分母）

| 折 | Static(D0–D2) | Group(D3) | Full(D0–D3) | B3 无条件关系 | B5 逐组机制 | **B4 单一最佳解释** |
|---|---|---|---|---|---|---|
| Rotterdam (n=46) | .4348/.4786 | .4348/.4786 | .4348/.4786 | .4348/.4786 | .4348/.4786 | **.4565/.4873 (h6)** |
| Shanghai (n=430) | .7070/.6825 | .7070/.6825 | .7070/.6825 | .7070/.6825 | .7070/.6825 | **.7233/.6964 (h2)** |
| Port Klang (n=9) | .1111/.0417 | 同左 | 同左 | 同左 | 同左 | 同左 (h3) |
| Fujairah (n=7) | .1429/.3333 | 同左 | 同左 | 同左 | 同左 | **.2857/.4167 (h5)** |

（数值为 Acc/BA；Static/Group/Full/B3/B5 四折全部逐位相同。）

## 结论

1. **知识侧变体（Static / Group / Full / B3 / B5）在任何一折都没有改变预测**：
   五个变体的 retained 都是 8/8（或 B3 的池化后仍 8/8），因此评估值逐位相同。
   ⇒ 本轮「场景知识」模块**实测增量为 0**，与 P1 的解释一致（活跃单元上的矩残差都 < tau）。
2. **唯一产生差异的是 B4（只留一个最佳解释）**，但它改变的是「用哪个候选头」，不是知识筛选；
   Rotterdam +.022 / Shanghai +.016 / Fujairah +.143 的差值在 n=46/430/7 下均不显著。
3. Static 与 Group 单独看也完全相同：D0 与 D3 各自单独启用时都保留全库，说明
   **当前可检验的两类知识（岸距、群体密度）在本批目标上无法区分候选**。
